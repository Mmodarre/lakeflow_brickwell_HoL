# Generated by LakehousePlumber - Orchestration Job for Brickwell_Health_LHP_Project

resources:
  jobs:
    Brickwell_Health_LHP_Project_orchestration:
      name: Brickwell_Health_LHP_Project_orchestration
      max_concurrent_runs: 1
      tasks:
        - task_key: bronze_pipeline_pipeline
          pipeline_task:
            pipeline_id: ${resources.pipelines.bronze_pipeline_pipeline.id}
            full_refresh: false
        - task_key: silver_pipeline_pipeline
          pipeline_task:
            pipeline_id: ${resources.pipelines.silver_pipeline_pipeline.id}
            full_refresh: false
        - task_key: gold_pipeline_pipeline
          depends_on:
            - task_key: silver_pipeline_pipeline
          pipeline_task:
            pipeline_id: ${resources.pipelines.gold_pipeline_pipeline.id}
            full_refresh: false
      queue:
        enabled: true
      performance_target: STANDARD

      # Additional job configuration options (add to you job_config.yaml as needed):

      # performance_target: PERFORMANCE_OPTIMIZED # Fast, performant compute startup and execution speed.

      # Enable job-level timeout
      # timeout_seconds: 3600

      # Job-level tags
      # tags:
      #   environment: dev
      #   pipeline_orchestration: "true"

      # Email notifications
      # email_notifications:
      #   on_start: []
      #   on_success: []
      #   on_failure: []

      # Webhook notifications
      # webhook_notifications:
      #   on_start: []
      #   on_success: []
      #   on_failure: []

      # Job permissions
      # permissions:
      #   - level: CAN_MANAGE
      #     user_name: "admin@company.com"
      #   - level: CAN_VIEW
      #     group_name: "data-team"

      # Schedule configuration (uncomment to enable)
      # schedule:
      #   quartz_cron_expression: "0 0 8 * * ?" # Daily at 8 AM
      #   timezone_id: "America/New_York"
      #   pause_status: UNPAUSED