pipeline: reference_bronze
flowgroup: reference_provider_TMPL003

use_template: TMPL003_scd_type1

template_parameters:
  source_table: "brickwell_health.reference_ingestion_schema.provider"
  target_table: "provider"
  natural_keys:
    - provider_number
  target_schema: "edw_bronze_reference"