pipeline: survey_bronze
flowgroup: survey_csat_survey_TMPL002

use_template: TMPL002_append_only

template_parameters:
  source_table: "brickwell_health.survey_ingestion_schema.csat_survey"
  target_table: "csat_survey"
  uuid_columns:
    - survey_id
    - policy_id
    - member_id
    - interaction_id
  target_schema: "edw_bronze_survey"