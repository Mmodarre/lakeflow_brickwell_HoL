pipeline: claim_bronze
flowgroup: claim_hospital_admission_TMPL004

use_template: TMPL004_cdc_log_scd2

template_parameters:
  source_table: "brickwell_health.ingest_schema_bwh.hospital_admission"
  target_table: "hospital_admission"
  natural_keys:
    - admission_id
  epoch_day_columns:
    - admission_date
    - discharge_date
  modified_at_column: "modified_at"
  created_at_column: "created_at"
  target_schema: "edw_bronze_claim"
