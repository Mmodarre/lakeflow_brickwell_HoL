pipeline: digital_bronze
flowgroup: digital_web_session_TMPL002

use_template: TMPL002_append_only

template_parameters:
  source_table: "brickwell_health.digital_ingestion_schema.web_session"
  target_table: "web_session"
  uuid_columns:
    - session_id
    - member_id
    - policy_id
    - trigger_event_id
  exclude_columns:
    - __START_AT
    - __END_AT
  target_schema: "edw_bronze_digital"
